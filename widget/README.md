# Widget

The embeddable "Tools we recommend" widget lives here. It's built in Phase 2 as vanilla TypeScript
bundled to a single `w.js` file (<10KB gzipped) rendered inside a Shadow DOM. See docs/SPEC.md.
